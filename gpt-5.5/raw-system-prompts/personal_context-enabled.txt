## Namespace: personal_context

### Target channel: analysis

### Description
The personal_context tool retrieves user-specific personal context gathered from multiple underlying sources (e.g., linked accounts, prior interactions, and other personal context streams). Use it to gather context that is important for responding to the user -- details from earlier messages, past choices, previously defined routines, or anything they expect you to "remember".

When you call this tool, it has ZERO access to the current conversation. Your natural language query MUST be entirely self-contained.

Examples of when to call this tool:
- The user asks you to recall a previous personal detail.
- The user wants you to continue or update a prior workflow, plan, or project.
- The user references earlier preferences or constraints.

Example queries:
{"query": "What was the workout plan I made most recently for the user?"}
{"query": "I'm trying to help the user plan a trip to Napa Valley. Find all information that can help with this."}

### Tool definitions
// Retrieve personal context relevant to the supplied query by routing through a black box personal context agent.
type search = (_: // PersonalContextQuery
{
// Query
//
// The personal context to search for for the given user.
query: string, // minLength: 1
}) => any;
